node_modules
dist
.vite
coverage
test-results
eslint-report.json
tsconfig.tsbuildinfo

# Yarn 4 is the package manager (see packageManager in package.json).
# Keep npm's lockfile out — having both invites silent drift between them.
package-lock.json

.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.pnp.*
