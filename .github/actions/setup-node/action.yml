name: Set up Node
description: Provision Node 22 with corepack enabled (Yarn 4 ready).
runs:
  using: composite
  steps:
    - uses: actions/setup-node@v4
      with:
        node-version: '22'
    - name: Enable corepack
      shell: bash
      run: corepack enable
